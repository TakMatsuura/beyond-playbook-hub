name: Weekly Report

on:
  schedule:
    # 毎週月曜 7:00 JST = 日曜 22:00 UTC
    - cron: '0 22 * * 0'
  workflow_dispatch:

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Send weekly report
        env:
          DAILY_REPORT_KEY: ${{ secrets.DAILY_REPORT_KEY }}
        run: |
          if [ -z "$DAILY_REPORT_KEY" ]; then
            echo "::error::DAILY_REPORT_KEY secret is missing"
            exit 1
          fi
          status=$(curl -sS -w "%{http_code}" -o /tmp/resp.json "https://playbook.beyond-holdings.co.jp/api/weekly-report?key=${DAILY_REPORT_KEY}")
          echo "HTTP: $status"
          cat /tmp/resp.json
          test "$status" = "200"
